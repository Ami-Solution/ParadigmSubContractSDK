contract('ZeroExSubContract', function(accounts) {
  it("should assert true", function(done) {
    var zero_ex_sub_contract = ZeroExSubContract.deployed();
    assert.isTrue(true);
    done();
  });
});
